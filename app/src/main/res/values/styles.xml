<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextNormalAppearance">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/size_body_note</item>
        <item name="android:layout_marginBottom">@dimen/margin_normal_text_view</item>
        <item name="android:layout_marginEnd">@dimen/margin_normal_text_view</item>
        <item name="android:layout_marginLeft">@dimen/margin_normal_text_view</item>
        <item name="android:layout_marginRight">@dimen/margin_normal_text_view</item>
        <item name="android:layout_marginStart">@dimen/margin_normal_text_view</item>
        <item name="android:layout_marginTop">@dimen/margin_normal_text_view</item>
    </style>

    <style name="TextTitleNoteAppearance">
        <item name="android:textSize">@dimen/size_title_note</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/margin_normal_text_view</item>
        <item name="android:layout_marginStart">@dimen/margin_normal_text_view</item>
        <item name="android:layout_marginTop">@dimen/margin_normal_text_view</item>
    </style>
</resources>
